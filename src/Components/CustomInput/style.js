import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  inpLarge: {
    width: '95%',
    textAlign: 'right',
    padding: 5,
    borderColor: '#CDC9C3',
    borderWidth: 1,
    borderRadius: 7,
    height: 45,
  },
});

export default styles;
